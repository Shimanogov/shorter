version: "3"

services:
  web:
    build:
      context: .
    ports:
      - "8000:8000"
  redis:
    image: redis

    # TODO: (big) qr generation
    # TODO: (big) make also work from URL line of browser
    # TODO: (big) generate favicon and css config from config.yaml